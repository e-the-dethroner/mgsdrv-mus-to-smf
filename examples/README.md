# Examples

These examples are synthetic placeholders for release packaging and config smoke testing.

They are not based on an existing song and should not be used as audio-quality reference material. Replace or extend them later with samples that have explicit permission from the original author.

```bash
cargo run -- examples/placeholder.mus -o target/placeholder.mid --config examples/placeholder.smfmap.yaml
```
